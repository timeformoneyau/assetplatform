# Anthropic API key — required for document parsing (POST /api/parse-document)
# Get one at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-...

# Hedera credentials — required for HCS anchoring (POST /api/anchor-record)
# Create a testnet account at https://portal.hedera.com
HEDERA_ACCOUNT_ID=0.0.XXXXXXX
HEDERA_PRIVATE_KEY=302e...
HEDERA_TOPIC_ID=0.0.XXXXXXX
HEDERA_NETWORK=testnet
